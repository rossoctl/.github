---
slug: /
sidebar_label: Welcome to rossoctl
title: Welcome to rossoctl
---

import Link from '@docusaurus/Link';
import Accordion from '@site/src/components/Accordion';

export const openQuestions = [
  {
    number: '01',
    title: 'Cryptographic identity and delegated access for every agent',
    description:
      "How does an agent prove who it is, what it's allowed to do, and on whose behalf it's acting — across user→agent, agent→agent, and agent→tool boundaries — without leaking credentials or breaking zero-trust assumptions?",
  },
  {
    number: '02',
    title: 'Framework-neutral deployment, scheduling, and state',
    description:
      'How do you deploy, schedule, and manage agents built on any framework — LangGraph, CrewAI, AG2, or custom — on Kubernetes, without changing agent code and without losing state across sessions or restarts?',
  },
  {
    number: '03',
    title: 'Bounded execution and network enforcement for autonomous agents',
    description:
      "Autonomous agents need bounded execution — constrained egress, isolated filesystems, session-scoped access — so a compromised or misbehaving agent can't exfiltrate data or move laterally across the platform.",
  },
  {
    number: '04',
    title: 'End-to-end traceability across inference, tools, and policy',
    description:
      'How do you correlate LLM calls, tool invocations, network hops, and authorization decisions into one causal trace — ideally without instrumenting agent code — and keep it useful for audit?',
  },
];

export const keyCapabilities = [
  {
    number: '01',
    title: 'SPIFFE/SPIRE workload identity',
    description:
      'Every agent gets a rotating cryptographic identity — no shared API keys or static secrets to leak.',
  },
  {
    number: '02',
    title: 'Token exchange — AuthBridge (RFC 8693)',
    description:
      "Agents act on a user's behalf with short-lived, audience-scoped tokens instead of standing credentials.",
  },
  {
    number: '03',
    title: 'Capability-intersection authorization',
    description:
      'An agent can only do what the user and the agent are both allowed to — never the union of the two.',
  },
  {
    number: '04',
    title: 'Container + VM sandbox isolation',
    description:
      'Run self-modifying or untrusted agents without risking the host or other tenants.',
  },
  {
    number: '05',
    title: 'MCP Gateway with auth injection',
    description:
      'Route every tool call through one authenticated, audited front door — agents never hold tool credentials.',
  },
  {
    number: '06',
    title: 'A2A protocol',
    description:
      'Agents discover and call each other across frameworks over an open standard.',
  },
  {
    number: '07',
    title: 'Multi-LLM proxy (LiteLLM)',
    description: 'Switch or mix 15+ model providers without touching agent code.',
  },
  {
    number: '08',
    title: 'Tracing — Phoenix + OpenTelemetry',
    description:
      'See every LLM call, tool call, and authorization decision correlated in one trace.',
  },
];

<div className="rosso-landing">

# A research project building the secure platform for AI agents on Kubernetes

<div className="rosso-intro">

rossoctl gives every AI agent a cryptographic identity, scoped access, and isolation — on Kubernetes, on infrastructure you control. It works with agents built in any framework, from LangGraph and CrewAI to any A2A-compatible container, and builds on open standards: A2A between agents, MCP for tools. Open source under Apache 2.0, started at IBM Research with Red Hat.

Deploying an agent is easy. Running an autonomous one — one that acts on its own, calls tools, and rewrites its own workspace — safely, on infrastructure you control, is not. That's the problem we work on, in the open.

</div>

<div className="margin-top--md margin-bottom--lg">
  <Link className="button button--lg cta-primary margin-right--sm" to="/docs/category/getting-started">Get started</Link>
  <Link className="button button--lg cta-secondary" to="/docs/category/concepts">Learn more</Link>
</div>

## Open questions

<Accordion items={openQuestions} redTitles />

## Key capabilities

<Accordion items={keyCapabilities} />

</div>
